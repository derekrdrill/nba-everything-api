import mongoose from 'mongoose';

export const PlayerGameStatsSchema = new mongoose.Schema({
  id: Number,
  min: String,
  fgm: Number,
  fga: Number,
  fg_pct: Number,
  fg3m: Number,
  fg3a: Number,
  fg3_pct: Number,
  ftm: Number,
  fta: Number,
  ft_pct: Number,
  oreb: Number,
  dreb: Number,
  reb: Number,
  ast: Number,
  stl: Number,
  blk: Number,
  turnover: Number,
  pf: Number,
  pts: Number,
  player: {
    id: Number,
    first_name: String,
    last_name: String,
    position: String,
    height: String,
    weight: String,
    jersey_number: String,
    college: String,
    country: String,
    draft_year: Number,
    draft_round: Number,
    draft_number: Number,
    team_id: Number,
  },
  team: {
    id: Number,
    conference: String,
    division: String,
    city: String,
    name: String,
    full_name: String,
    abbreviation: String,
  },
  game: {
    id: Number,
    date: String,
    season: Number,
    status: String,
    period: Number,
    time: String,
    postseason: Boolean,
    home_team_score: Number,
    visitor_team_score: Number,
    datetime: Date,
    home_team_id: Number,
    visitor_team_id: Number,
  },
});

export const PlayerGameStats =
  mongoose.models?.['player-game-stats'] ||
  mongoose.model('player-game-stats', PlayerGameStatsSchema);
